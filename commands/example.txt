if you want to add anything other functions that belong in a different category form the rest

module.exports = {
	func1: function(message, args) {
		code goes here
	},
	func2: function(message, args) {
		code goes here
	},
	func3: function(message, args) {
		code goes here
	}
}
